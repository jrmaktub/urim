[install]
# Bun install configuration to avoid native module issues
auto = "fallback"

[install.scopes]
# Make all Trezor packages optional
"@trezor" = "optional"
"@ledgerhq" = "optional"

[install.trustedDependencies]
# Don't trust any packages to run lifecycle scripts
